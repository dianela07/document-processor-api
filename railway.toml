[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/v1/info"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
